services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - "./.env:/app/.env:ro"
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3